# ─────────────────────────────────────────────────────────────────────────────
# Hybrid Search RAG Engine — Pinned Dependencies
# Python 3.11+ required
# ─────────────────────────────────────────────────────────────────────────────

# ── LangChain Ecosystem ──────────────────────────────────────────────────────
langchain==1.2.12                  # Core orchestration, RetrievalQA chain
langchain-openai==1.1.11           # ChatOpenAI + OpenAIEmbeddings wrappers
langchain-community==0.4.1         # FAISS VectorStore integration
langchain-text-splitters==0.3.8    # SemanticChunker lives here
langchain-experimental==0.3.4      # Extra experimental splitters / chains

# ── OpenAI SDK ───────────────────────────────────────────────────────────────
openai==2.26.0                     # text-embedding-3-small + GPT-4o calls
tiktoken==0.9.0                    # Token counting for batching / prompts

# ── Vector Search ────────────────────────────────────────────────────────────
faiss-cpu==1.9.0.post1             # Dense ANN index (CPU-only build)
numpy==1.26.4                      # Required by FAISS; keep on 1.x for compat

# ── Keyword Search ───────────────────────────────────────────────────────────
rank-bm25==0.2.2                   # BM25Okapi sparse retriever

# ── Document Loaders ─────────────────────────────────────────────────────────
pypdf==5.9.0                       # PDF parsing (page-by-page extraction)
python-docx==1.2.0                 # DOCX parsing

# ── API Layer ────────────────────────────────────────────────────────────────
fastapi==0.115.14                  # REST framework (stable LTS series)
uvicorn[standard]==0.34.3          # ASGI server with uvloop + httptools
python-multipart==0.0.22           # Required for FastAPI file uploads
httpx==0.28.1                      # Async HTTP client (TestClient + OpenAI)

# ── Data Validation ──────────────────────────────────────────────────────────
pydantic==2.11.4                   # Request/Response schemas, settings
pydantic-settings==2.9.1           # BaseSettings for .env loading

# ── Resilience & Config ──────────────────────────────────────────────────────
tenacity==9.1.4                    # Retry logic with exponential back-off
python-dotenv==1.2.2               # Load .env into os.environ

# ── Serialization ────────────────────────────────────────────────────────────
pickle5==0.0.12; python_version < "3.11"  # pickle protocol 5 back-port (not needed on 3.11+)

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.5
pytest-asyncio==0.26.0
pytest-cov==6.1.0                  # Coverage reports
